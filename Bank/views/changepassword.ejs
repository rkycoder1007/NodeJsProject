<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <style>
        body{
            background-color: blue;
            width: 100%;
            position: relative;
        }
        html{
            font-size:10px;
        }
        .info{
            width:fit-content;
            background-color: wheat;
            display: flex;
            flex-direction: column;
            gap:1rem;
            padding:2rem;
            border-radius: 0.5rem;
            position:absolute;
            left:50%;
            transform: translateX(-50%);
            margin:auto;
        }
        .info h2{
            font-size:4rem;
            text-align: center;
            color:orange;
            margin-bottom: 2rem;
        }
        .info form{
            display: flex;
            flex-direction: column;
            gap:1rem;
        }
        .info form input{
            padding:0.5rem 1rem;
            font-size:2rem;
            border-radius: 0.5rem;
            border:none;
            overflow: hidden;
        }
        .info form input:focus{
            background-color: aqua;
        }
        .info form .submit{
            width:fit-content;
            background-color: green;
            padding:0.5rem 2rem;
            color:white;
            font-weight: 700;
            align-self: center;
            cursor: pointer;
        }
        .info form .submit:hover{
            background-color: aliceblue;
            color:orange;
        }
    </style>
</head>
<body>
    <%- include("./partials/menuhead") %>
    <div class="info">
        <h2>Change Password</h2>
        <form action="changepassword" method="POST">
            <input type="email" name="email" id="email" placeholder="Email ID" required>
            <input type="password" name="currentpassword" id="oldpassword" placeholder="Current Password" required>
            <input type="password" name="newpassword" id="newpassword" placeholder="New Password" minlength="8" required>
            <input type="password" name="cpassword" id="cpassword" placeholder="Confirm Password" required>
            <input type="submit" class="submit" value="Change Password">
        </form>
    </div>
</body>
</html>